apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-internal-only
spec:
  podSelector:
    matchLabels:
      app: calc-api
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector: {}